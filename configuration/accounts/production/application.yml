server:
  port: 7777

accounts:
  datasource:
    url: jdbc:mysql://localhost:3306/account?serverTimezone=Europe/Warsaw&useSSL=false&allowPublicKeyRetrieval=true
    username: cezaryjanicki
    password: cezaryjanicki00
    ddl-mode: update